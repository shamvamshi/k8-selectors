apiVersion: v1
kind: Pod
metadata:
  name: node-selector-demo-1
  labels:
    env: test
spec:
  containers:
  - name: nginx
    image: nginx
    imagePullPolicy: Always
  nodeSelector:
    disktype: ssd
  tolerations:
  - key: "key1"
    value: "value1"
    effect: "NoExeute"
    operator: "Equal"
  - key: "key1"
    value: "value1"
    effect: "NoSchedule"
    operator: "Equal"