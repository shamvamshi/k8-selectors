apiVersion: v1
kind: Pod
metadata:
  name: node-affinity
spec:
  affinity:
    nodeAffinity:
      # requiredDuringSchedulingIgnoredDuringExecution: # this is hard rule
      #   nodeSelectorTerms:
      #   - matchExpressions:
      #     - key: hardware
      #       operator: In
      #       values:
      #       # - gpu
      #         - gpuuu
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100 # High Weight (e.g., 90-100): "I really, really want this." ,,,,Low Weight (e.g., 1-10)
          preference:
            matchExpressions:
            - key: hardware
              operator: In
              values:
              - gpuu
  containers:
  - name: node-affinity
    image: nginx